
// Escreva uma função chamada ePaisagem que
// que recebe dois argumentos, largura e altura
// de uma imagem (number).
// Reltorne true se a imagem estiver no modo
// paisagem.

const ePaisagem = (largura,Altura) => (largura>Altura);//ja vai retorna true ou false
console.log(ePaisagem(1900,1080));